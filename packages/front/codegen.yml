overwrite: true
schema: "http://localhost:7501/graphql"
documents: "graphql/**/*.ts"
generates:
  graphql/generated.ts:
    config:
      enumsAsConst: true
    plugins:
      - "typescript"
      - "typescript-operations"
